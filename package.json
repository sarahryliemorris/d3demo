{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache", 
    "start": "http-server", 
    "build": "echo 'No build required'", 
    "dev": "http-server -o", 
    "deploy": "echo 'Deploying the static site...'", 
    "install": "pip install -r requirements.txt" 
  },
  "dependencies": {
    "@observablehq/framework": "^1.11.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "http-server": "^14.1.1" 
  },
  "engines": {
    "node": ">=18"
  }
}
